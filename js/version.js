/* 앱 버전 — 텔레메트리 태그·풋터 표기용.
   ⚠️ 배포 시 sw.js의 CACHE와 같은 커밋에서 함께 올린다(두 파일, 같은 버전).
   sw.js가 이 파일을 importScripts 하지는 않는다 — SW 갱신 감지는 sw.js 본체
   바이트 변경(모든 브라우저 보장)에 의존해야 하기 때문(적대 검토 2026-07-29). */
var APP_VER = '7.11.0';
