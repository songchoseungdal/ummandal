# 엄만달 UI 가이드 (정본)

> **UI를 만지는 모든 작업은 이 문서를 먼저 읽고, 여기 있는 규칙만 따른다.**
> 여기 없는 색·크기·패턴을 새로 만들고 싶으면 — 먼저 이 문서에 추가하고 나서 쓴다(문서가 정본).
> 모든 수치는 `css/style.css`에 실존하는 값에서 추출했다. (제정 2026-07-30)

## 0. 철학 — 판정 질문 (잡스 원칙, 매 화면 채점)

UI 작업을 마치면 아래 6개 질문으로 자체 채점한다. 하나라도 "아니오"면 완성이 아니다.

| # | 판정 질문 | 원칙 |
|---|---|---|
| 1 | 이 화면에서 **뺄 수 있는 요소가 더 없는가?** | 단순함 = 빼기. 좋은 아이디어에 '아니오'라고 말하기 |
| 2 | 처음 본 사람이 **설명 없이 다음 행동을 알 수 있는가?** | 설명서가 필요 없어야 한다 (It just works) |
| 3 | **핵심 행동이 1개**이고, 화면에서 가장 크게 보이는가? | 집중. 한 화면 = 한 가지 일 |
| 4 | 어디서든 **한 터치로 홈(달력)에 돌아갈 수 있는가?** | 홈버튼 하나의 철학 — 길 잃는 공포 제거 |
| 5 | 글자가 **정해진 타이포 단계(§2) 안에만** 있는가? | 타이포그래피가 곧 품격 |
| 6 | 이 화면이 **다른 화면과 같은 사람이 만든 것처럼** 보이는가? | 보이지 않는 뒷면도 아름답게 — 전 화면 일관성 |

사용자 = 40~60대 여성 간호사(수간호사). 큰 글씨·높은 대비·적은 단계·일상어. 이 문서의 모든 수치는 이 사용자 기준이다.

## 1. 색 토큰 사전 (`:root` — 새 색상 하드코딩 금지)

| 토큰 | 용도 |
|---|---|
| `--bg` / `--card` / `--card2` | 화면 배경 / 카드 흰색 / **눌림(:active) 배경** |
| `--line` | 모든 테두리·구분선 (다른 회색 선 금지) |
| `--ink` / `--sub` / `--faint` | 본문 / 보조 설명 / 흐린 캡션 — 글자색은 이 3개가 기본 |
| `--brand` / `--brand-dk` / `--brand-soft` | 보라(주 행동·선택 상태) / 진한 보라 / 연보라 배경 |
| `--danger`·`--ok`·`--warn` + `-soft` | 오류·성공·주의. 진한 색=글자/테두리, soft=배경 |
| `--warn-ink` | `--warn-soft` 배경 위에 올리는 글자색(#8a5d00) — `--warn`은 밝아서 글자로 쓰면 대비가 부족하다 (2026-07-30 등재) |
| `--d`·`--e`·`--n`·`--o-*`·`--v-*`·`--moon` | 근무 코드 색(데이·이브닝·나이트·오프·연차·희망휴무) — **의미 변경 금지** |

- 강조색은 화면당 1개(보라). 빨강은 위험·오류에만, 초록은 성공·통과에만.
- 색만으로 의미를 전달하지 않는다 — 글자·아이콘을 병기 (색약 대비).

## 2. 타이포 단계 (5단계 고정 — 이 밖의 크기 금지)

| 단계 | 크기/굵기 | 용도 (실제 클래스) |
|---|---|---|
| 화면 제목 | **30px / 800** | `.pagetitle` (인증 화면은 26px `.auth-title`) |
| 섹션 제목 | **20~22px / 700~800** | `h2` 21px, 시트 제목 `.sh-head h3` 20px, 부속화면 `.ss-title` 19px |
| 본문·버튼 | **17px / 400~700** | body 기본, `.btn`, `.listrow`, 입력값 |
| 보조 | **15~16.5px / 400~600** | `.hint` 15px, `.pagesub` 16.5px, 행 부제 14.5~15px |
| 캡션 | **13~14px / 600** | 범례·버전·각주. **13px 미만은 본문 UI에 금지**(그리드 셀 예외) |

- 행간 1.5 (`body`), 제목은 1.1~1.25. `word-break: keep-all` 유지.
- 굵기는 400(본문)·600(준강조)·700(버튼·강조)·800(제목) 4개만.
- 글꼴은 system-ui 스택 하나 — 새 글꼴 도입 금지.

## 3. 간격·모서리·터치

- **간격 스케일**: 6 / 10 / 14 / 20px 을 기본으로 쓴다. 카드 안 여백 20px, 카드 사이 14px, 버튼·요소 사이 10px, 미세 간격 6px.
- **본문 폭**: `main` max-width 720px(PC 860px), 좌우 padding 20px.
- **모서리 반경 사다리**: 999px(칩·원형 버튼) > 28px(바텀시트 상단) > 18px(카드) > 14~16px(버튼·목록 행·입력) > 8~12px(작은 칩·미니 요소). 새 반경 값 만들지 않기.
- **터치 영역 최소 44×44px** — 아이콘 버튼·달력 날짜·그리드 셀 전부. (예외: 스테퍼 ± 34px — 이미 확정된 예외)
- 화면 하단 고정 요소는 `env(safe-area-inset-bottom)` 패딩 필수.

## 4. 컴포넌트 — 새로 만들지 말고 이걸 재사용

| 필요한 것 | 쓰는 클래스 | 규칙 |
|---|---|---|
| 정보 묶음 | `.card` | 흰 배경 + 18px 반경 + `--line` 테두리 |
| 주 행동 버튼 | `.btn.big` (전폭은 `.big.xl`) | **화면당 1개**, 보라. 두 버튼 나란히면 `.btnrow`(취소=`.gray`) |
| 보조 버튼 | `.btn.gray` / `.btn.outline` | 회색 면 / 보라 테두리 |
| 위험 행동 | `.btn.warn`·`.sh-danger`·`.logoutbtn` | 빨강 글자, 면 없음. 확인 단계 필수 |
| 이동형 목록 행 | `.listrow` (아이콘+제목+부제+꺾쇠) | 행 전체가 터치 대상 |
| 설정형 행 | `.datarow`·`.rrow` (카드 안 구분선 행) | 카드 하나에 관련 행만 묶기 |
| 선택지 2~3개 | `.seg`(분할 버튼) / `.radiocard`(설명 있는 선택) | 선택 상태 = 보라 테두리+연보라 배경 |
| 켜고 끄기 | `.tgl` 토글 | |
| 수량 조절 | `.stepper` | 직접 입력 대신 ± 버튼 |
| 하위 입력·편집 | 바텀시트 `#sheet` | 그립+제목+**하단 sticky `.btnrow`**(버튼이 스크롤에 숨으면 안 됨) |
| 전체 화면 부속 | `.subscreen` | 상단 뒤로가기(`.ss-back` 46px)+가운데 제목 |
| 결과 알림 | `#toast` | 1~2초 지나가는 확인. 판단 요구 금지 |
| 긴 명단 그룹 | `.glbtn`(그룹 머리글 버튼) | 접기/펼치기 — 기본: **그룹 2개 이상이면 접힘**(그룹끼리 한눈에, 2026-07-30 초승달 결정)·하나뿐이면 펼침. 오른쪽에 「접기 ▴/펼치기 ▾」 글자로 상태 표시, 머리글에 인원 수 필수(접혀도 내용 암시) |
| 명단 검색 | `.wardsearch` | 명단이 한 화면을 넘길 즈음(7명+)부터만 노출 — 적을 땐 군더더기 |
| 상태 배너 | `#banner` ok/bad | 홈 위반 요약 등 지속 상태 |
| 보류 알림띠 | `#syncBanner.warn` | **비정상 상태에서만** 뜨는 홈 맨 위 한 줄(서버에 못 올림). 평소엔 `display:none`이라 자리를 차지하지 않는다. 누르면 원인을 볼 수 있는 화면으로 이동. 뜨는 동안 `body.syncwarn`이 붙어 준비화면 부제(`.pagesub`)를 접는다 — **알림을 추가하면 그만큼 무엇을 접을지 함께 정한다**(한 화면 규칙 §5, 384×694 실측: 접지 않으면 「만들기」 버튼이 탭에 가림) |
| 상태 칩 | `.chip` red/green/plain | |

## 5. 행동(인터랙션) 규칙 — 디자인은 작동 방식이다

- **눌림 피드백 필수**: 면이 있는 요소 `:active` = `--card2` 배경(또는 brightness .94), 면이 없으면 scale(.98).
- **포커스**: 입력 focus = `outline: 2px solid var(--brand)`.
- **되돌릴 수 있게**: 파괴적 행동(삭제·초기화)은 즉시 실행하지 않고 확인을 거친다. 실수 방지가 안내문보다 우선.
- **항상 보이는 완료 버튼**: 긴 시트·화면의 적용/저장 줄은 sticky — "골라도 안 바뀐다" 방지.
- **애니메이션은 짧고 기능적으로**: 0.15~0.25s, 등장·상태 변화에만. `prefers-reduced-motion` 존중. 장식 애니메이션 금지.
- **대기 상태는 진행을 보여준다**: 스피너+단계 텍스트(`#genScreen`·`#aiLoading` 패턴).
- **핵심 행동은 첫 화면 안에**: 주 행동 버튼이 스크롤 아래 잠기면 안 됨 — 여백을 조여 맞추고(글자 크기는 유지), 안 되면 sticky. 아주 낮은 화면에서만 제목 한 단계 축소 허용(`@media (max-height: 680px), (max-width: 370px) and (max-height: 740px)` 패턴 — 기준값은 실기기·실뷰포트 실측으로 보정한다. 740 단독은 384×694 폰에서 과잉 발동, 680 단독은 360×720에서 버튼 가림. 2026-07-30).
- **스크롤바는 브랜드색**: 6px, `rgba(106,75,196,.45)`(=--brand 45%) + 트랙 투명 — 지금 어디쯤인지 보이게. (안드로이드 크롬은 본문 스크롤바 커스텀을 무시할 수 있음 — 내부 스크롤 영역 기준)

## 6. 금지 목록

1. 색·크기·반경·간격 **하드코딩 신설 금지** — 토큰과 §2·§3 스케일만.
2. 화면당 주 행동 버튼(보라 면) 2개 이상 금지.
3. 13px 미만 본문 글자 금지(그리드 셀·미니맵 예외).
4. 44px 미만 터치 대상 금지(스테퍼 예외).
5. 전문용어·영어 UI 문구 금지 — 일상어로("동기화 실패" → "저장이 안 됐어요").
6. 새 글꼴·새 강조색·장식 그림자 도입 금지.
7. 가로 넘침 금지 — 배포 전 320·412px 폭에서 `body.scrollWidth <= innerWidth` 확인(CLAUDE.md 반응형 QA).

## 7. 작업 절차

1. 시작: 이 문서 + 비슷한 기존 화면 1개를 보고 패턴을 정한다. (GPT `UI_SPEC.md`·목업은 화면별 스펙 참고 — 충돌 시 UI_SPEC 문구 우선, 공통 규칙은 이 문서 우선)
2. 구현: §4 컴포넌트 재사용 → 없으면 §1~3 규칙 안에서 신설 + **이 문서에 추가**.
3. 완료 전: §0 판정 질문 6개 채점 + §6 금지 목록 점검.
